language: python

sudo: false

python:
    - 2.7

cache:
  directories:
    - $HOME/.cache/pip

install:
    - pip install -U pip wheel
    - pip install tox

script:
    - make lint-code
    - tox

branches:
  only:
    - master
